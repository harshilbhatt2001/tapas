# Devenv
.devenv*
devenv.local.nix
devenv.local.yaml

# direnv
.direnv

# pre-commit
.pre-commit-config.yaml

# generated by devenv claude.code
.mcp.json
.claude/settings.json
.claude/agents/
.claude/worktrees/
.worktrees/

# rust
/target

# nix
result
result-*

# Google OAuth client and token caches (tapas keeps them in its config dir; never commit copies)
client_secret*.json
tokens*.json
.env*
